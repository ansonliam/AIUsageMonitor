<Application x:Class="AIUsageMonitor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <!-- Keep the application default light, like AngesnHardwareMonitor. The Windows
                     widget explicitly paints its dark cards and text, while Settings explicitly
                     paints its light dialog palette, so their visible surfaces stay unchanged.
                     The Windows widget's right-click menu intentionally follows this light
                     default. -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Teal.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Window">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ThemeForeground}" />
                <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            </Style>
            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Padding" Value="9,3" />
                <Setter Property="MinHeight" Value="24" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
